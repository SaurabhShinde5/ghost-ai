# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Frontend wiring — the editor home now reads real project data and the dialogs drive the project CRUD API.

## Current Goal

- Implemented `context/feature-specs/07-wire-editor-home.md`: the editor home is server-rendered from live project data and the create/rename/delete dialogs call the real `/api/projects` routes. Next step is fleshing out the `/editor/[roomId]` workspace route that create navigates to (canvas + AI sidebar).

## Completed

- `07-wire-editor-home` — editor home wired to the real project API. `app/editor/page.tsx` is now a server component that awaits `getUserProjects()` and renders the new client shell (no client-side fetch on initial load). `lib/projects.ts`: `getUserProjects()` data helper returns `{ owned, shared }` — `owned` via `where: { ownerId: userId }`, `shared` via `collaborators: { some: { email: { in: emails } } }` with `ownerId: { not: userId }` (emails resolved from Clerk `currentUser()`), both `orderBy createdAt desc`, mapped to the UI `Project` shape (`toClientProject`, `role` = `owner`/`collaborator`, `slug` = `slugify(name)`); returns empty lists when unauthenticated. `components/editor/editor-shell.tsx`: extracted the former page body into a `"use client"` shell taking `ownedProjects`/`sharedProjects`, owning sidebar-open state + the `useProjectActions` hook, and the `lg:hidden` backdrop. `hooks/use-project-actions.ts` (replaces the deleted mock `hooks/use-project-dialogs.ts`): same dialog/form/loading surface, now with real async mutations — `submitCreate` derives `roomId = slugify(name)-<suffix>` (`generateSuffix()` = base36 random), POSTs `{ id: roomId, name }`, and `router.push(\`/editor/${roomId}\`)`; `submitRename` PATCHes then `router.refresh()`; `submitDelete` DELETEs then redirects to `/editor` when `usePathname()` equals the deleted workspace, else `router.refresh()`. All three guard on `isSubmitting`/empty input and clear the loading flag on non-ok/thrown responses. `components/editor/project-dialogs.tsx`: prop/type renamed `dialogs`→`actions` (`UseProjectActions`); the create preview now reads "Room ID: {slug}-…". `app/api/projects/route.ts` POST now accepts an optional client-provided `id` (trimmed non-empty string) so `project.id === roomId`, falling back to the cuid default when absent. Deleted `lib/mock-projects.ts`. Verified with `npm run build` (/editor now renders dynamically) and `eslint`.

- `06-project-apis` — backend project CRUD routes (no UI wiring). `app/api/projects/route.ts`: `GET` lists the authenticated user's owned projects (`where: { ownerId: userId }`, `orderBy createdAt desc`); `POST` creates a project with `ownerId = userId`, tolerantly parses the JSON body, defaults a missing/blank `name` to `Untitled Project`, uses the schema's cuid `id` (no sequential IDs), returns `201`. `app/api/projects/[projectId]/route.ts`: `PATCH` renames (validates non-empty `name`, `400` if blank) and `DELETE` deletes; both `findUnique` the project, return `404` if absent, and enforce owner-only mutation — `403` when `ownerId !== userId`. Every handler returns `401` when `auth()` yields no `userId` (defense-in-depth alongside `proxy.ts`, which already protects `/api/*`). Params typed via the Next.js 16 `RouteContext<'/api/projects/[projectId]'>` helper (async `params`). Per spec, only the owner (not collaborators) may rename/delete, which is intentionally stricter than `architecture-context.md`'s general collaborator-mutation rule. Root-cause fix in `lib/prisma.ts`: `createPrismaClient` now returns a single `PrismaClient` type (the Accelerate branch cast to it) — previously the union of the two transport client types made model methods resolve to incompatible signatures the moment a real query was type-checked. Verified with `npm run build` and `eslint`.

- `05-prisma` — Prisma v7 data layer landed. `prisma/models/project.prisma` (multi-file schema; `prisma.config.ts` reads `schema: "prisma/"`): `ProjectStatus` enum (`DRAFT`/`ARCHIVED`); `Project` (`id` cuid, `ownerId` → Clerk user, `name`, optional `description`, `status` default `DRAFT`, optional `canvasJsonPath` for the future Vercel Blob canvas reference, `createdAt`/`updatedAt`, `@@index([ownerId])` + `@@index([createdAt])`); `ProjectCollaborator` (`id` cuid, `projectId` relation with `onDelete: Cascade`, `email`, `createdAt`, `@@unique([projectId, email])`, `@@index([email])` + `@@index([projectId, createdAt])`). `lib/prisma.ts`: cached singleton exporting one `prisma` instance — `createPrismaClient(url)` branches on the protocol (`prisma+postgres://` → `new PrismaClient({ accelerateUrl }).$extends(withAccelerate())`; otherwise `new PrismaClient({ adapter: new PrismaPg({ connectionString }) })`), throws if `DATABASE_URL` is unset, and caches on `globalThis` outside production for hot-reload safety. Client generated to `app/generated/prisma` (gitignored). Migration `20260703095741_init_project_models` created and applied to the Prisma Postgres database (enum + both tables + all indexes + cascade FK verified in `migration.sql`). Added `@prisma/extension-accelerate` (the canonical Accelerate package) so the `prisma+postgres://` branch resolves; the active `DATABASE_URL` is a direct `postgres://` URL, so the `pg` adapter branch is what runs. Verified with `npm run build` and `eslint`.

- `04-project-dialogs` — editor home + project dialogs built on mock data (no persistence). `components/editor/editor-home.tsx`: centered heading (`Create a project or open an existing one`), description, and a `New Project` button with a `Plus` icon — no cards, minimal layout. `hooks/use-project-dialogs.ts`: a dedicated hook owning dialog state (`activeDialog: "create" | "rename" | "delete" | null`), the target project, form `name`, a live `slug` derived via `slugify(name)`, and `isSubmitting` loading state; submits are mocked (toggle loading + close) so real async can drop in later. `lib/slug.ts`: `slugify()` for the live slug preview. `types/project.ts`: `Project` interface + `ProjectRole` + `isOwnedProject()`. `lib/mock-projects.ts`: `MOCK_PROJECTS` (two owned, one collaborator). `components/editor/project-dialogs.tsx`: renders the three dialogs off the hook via the existing `EditorDialog` pattern — Create has a name input + live slug preview and a form-bound submit; Rename prefills + auto-focuses the input, shows the current name in the description, and submits on Enter (form submit); Delete is a destructive confirmation with no input and a `destructive`-variant confirm button. `components/editor/project-sidebar.tsx`: now takes `ownedProjects` / `sharedProjects` and the create/rename/delete handlers; My Projects lists owned projects with hover/focus-revealed `Pencil`/`Trash2` actions, Shared lists collaborator projects with no actions; empty states retained for empty lists. `app/editor/page.tsx`: instantiates the hook, mounts `EditorHome`, wires the sidebar `New Project` + home button + item actions to the dialogs, and adds a `lg:hidden` backdrop scrim (below the sidebar, above content) that closes the sidebar on tap. Verified with `tsc --noEmit` and `eslint`.
- `03-auth` — Clerk wired end to end. `app/layout.tsx`: root wrapped in `ClerkProvider` using the `dark` theme from `@clerk/ui/themes`, with `appearance.variables` overridden entirely through the app's CSS variables (`var(--accent-primary)`, `var(--bg-surface)`, `var(--text-primary)`, `var(--radius)`, etc.) — no hardcoded colors. `proxy.ts` at the project root (Next.js 16 renamed `middleware` → `proxy`): `clerkMiddleware` + `createRouteMatcher` treats only the sign-in/sign-up URL env vars as public and calls `auth.protect()` on everything else. `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` render Clerk's `SignIn`/`SignUp` inside a shared `components/auth/auth-shell.tsx` — an even 50/50 two-panel layout. Left panel (hidden below `lg`) is a brand-tinted surface (`bg-surface` + a faint `--accent-primary-dim` radial glow) holding a `Ghost` logo mark, a hero heading + tagline, an icon-led feature list (`Sparkles`/`Share2`/`FileText` in `bg-accent-dim` rounded squares), and a footer copyright; right panel is the centered Clerk form on `bg-base`. Typography uses the Geist font tokens (`font-sans`) per the UI guidelines. Design section of `03-auth.md` was updated to match this approved screenshot direction (the earlier "no gradients / no hero / no feature cards" constraints were relaxed). `app/page.tsx` is now a server component that reads `auth()` and redirects authenticated users to `/editor`, unauthenticated to `/sign-in`. `components/editor/editor-navbar.tsx` right section now holds Clerk's `UserButton`. Added a minimal `app/editor/page.tsx` (client) that mounts the existing `EditorNavbar` + `ProjectSidebar` so the redirect target exists and the `UserButton` renders in context. Added Clerk's standard `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` env vars (`/sign-in`, `/sign-up`). Installed `@clerk/ui`. Verified with `next build` and `eslint`.
- `02-editor-chrome` — base editor chrome built. `components/editor/editor-navbar.tsx`: fixed-height (`h-14`) top bar, left/center/right sections, ghost sidebar-toggle button swapping `PanelLeftOpen`/`PanelLeftClose` on the `isSidebarOpen` prop, `bg-surface` + subtle bottom border, right section empty. `components/editor/project-sidebar.tsx`: floating overlay (`absolute inset-y-0 left-0`, does not push content) that slides in via `translate-x` on the `isOpen` prop, "Projects" header + close button, shadcn `Tabs` (My Projects / Shared) each with an empty placeholder state, full-width `New Project` button with `Plus` icon at the bottom. `components/editor/editor-dialog.tsx`: reusable dialog pattern wrapping shadcn `Dialog` with title/description/footer slots, `rounded-3xl` and `globals.css` color tokens — ready for future use, no concrete dialogs wired yet. Verified with `tsc --noEmit` and `eslint`.
- `01-design-system` — shadcn/ui installed and configured (radix + Nova preset, `components.json`, RSC). Added primitives: Button, Card, Dialog, Input, Tabs, Textarea, ScrollArea (`components/ui/*`, unmodified). `lucide-react` installed. `lib/utils.ts` exposes `cn()`. `app/globals.css` defines the dark-only token layer from `ui-context.md`; app is dark by default (`dark` class on `<html>`). Verified with `tsc --noEmit` and `next build`.

## In Progress

- None.

## Next Up

- Build the `/editor/[roomId]` workspace route that `submitCreate` navigates to: the center canvas surface and the right-side AI sidebar. Until it exists, create/open navigation lands on a 404.

## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- Route protection lives in `proxy.ts` at the project root, not `middleware.ts` — Next.js 16 renamed Middleware to Proxy. `clerkMiddleware` is protected-by-default: only the sign-in/sign-up URL env vars are public.
- Clerk appearance is themed via `dark` from `@clerk/ui/themes` (not `@clerk/themes`) with all `variables` pointing at the app's existing CSS custom properties, so Clerk UI inherits the design tokens with zero hardcoded colors.
- Public-route config keys off Clerk's standard `NEXT_PUBLIC_CLERK_SIGN_IN_URL` / `NEXT_PUBLIC_CLERK_SIGN_UP_URL` env vars rather than string literals, so auth paths stay single-sourced.
- Dark-only theme: shadcn's light `:root` / dark `.dark` split is collapsed into a single dark palette in `globals.css` (values from `ui-context.md`), and `<html>` carries the `dark` class so shadcn `dark:` variants stay active. No light mode.
- Project UI mock data lives in `lib/mock-projects.ts` (not the legacy `data/` directory, which `architecture-context.md` marks unused for new artifacts); the shared `Project` type lives in `types/project.ts`. Both are placeholders until Prisma-backed project CRUD lands.
- Dialog/form/loading state plus the create/rename/delete mutations are centralized in `hooks/use-project-actions.ts` (formerly the mock `use-project-dialogs.ts`). The `/editor` page is a thin server component that fetches data and delegates all interactivity to `components/editor/editor-shell.tsx`, keeping the server/client boundary clean while composing home + sidebar + dialogs against one state source.
- `getUserProjects()` (`lib/projects.ts`) is the single server-side read path for the editor home, sharing the owned-projects query shape with `GET /api/projects`. Shared projects are resolved by matching the Clerk user's email addresses (via `currentUser()`) against `ProjectCollaborator.email`, since collaborators are keyed by email, not Clerk user ID.
- Project id / Liveblocks room id alignment: the create flow generates `roomId = slugify(name)-<suffix>` on the client and sends it as the project `id`; `POST /api/projects` uses a client-provided `id` when present (else the cuid default). This keeps `project.id === roomId` so the workspace route and canvas room resolve from one identifier.
- Prisma v7 with the `prisma-client` generator (ESM output to `app/generated/prisma`, gitignored) and required driver adapters. The datasource URL is single-sourced through `prisma.config.ts` (`datasource.url = process.env.DATABASE_URL`) rather than in `schema.prisma`. `lib/prisma.ts` picks the transport by URL protocol so a `prisma+postgres://` (Accelerate) or a direct `postgres://` (pg adapter) URL both work with no code change. Schema is split multi-file: connector/generator in `prisma/schema.prisma`, models in `prisma/models/*.prisma`.
- Project mutation routes (`PATCH`/`DELETE`) are owner-only per `06-project-apis.md` (non-owner → `403`), intentionally stricter than `architecture-context.md`'s general "owner or collaborator can mutate" rule. Collaborator write access can be reintroduced when the collaborator feature is actually built.
- `lib/prisma.ts` normalises `createPrismaClient` to a single `PrismaClient` return type (Accelerate branch cast to it). The prior union of the two transport client types was fine until a real model query was type-checked, at which point member signatures became incompatible; pinning the type is the root-cause fix and keeps both transports interchangeable at the call site.
- Design tokens from `ui-context.md` are exposed as Tailwind utilities via `@theme inline` (e.g. `bg-base`, `bg-surface`, `text-copy-*`, `text-brand`, `bg-accent-dim`, `text-ai`), layered alongside shadcn's semantic tokens.

## Session Notes

- Add context needed to resume work in the next session.
